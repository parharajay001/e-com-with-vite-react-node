import { Typography } from '@workspace/ui/src/lib/mui';
import { DashboardLayout } from '@workspace/ui';

const Settings = () => {
  return (
    <DashboardLayout title='Settings Management'>
      <Typography component='p'>Settings list will be displayed here</Typography>
    </DashboardLayout>
  );
};

export default Settings;
